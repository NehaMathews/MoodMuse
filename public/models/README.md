Place `face-api.js` TinyFaceDetector and FaceExpressionNet model files here to enable real webcam expression detection.

The app falls back to an animated local confidence engine when these files are absent, so demos still work immediately.
